function building = createBuildings(cfg)

building.position = cfg.building.position;

building.length = cfg.building.length;

building.width = cfg.building.width;

building.height = cfg.building.height;

end